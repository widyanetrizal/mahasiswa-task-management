server {
    listen 80;

    # Frontend

    root /usr/share/nginx/html;
    index index.html;

    # Cache file static
    location /static/ {
        expires 1y;
        add_header Cache-Control "public";
    }

    # SPA fallback
    location / {
        try_files $uri /index.html;
    }

    # Batas upload jika perlu
    client_max_body_size 2000M;

    # Backend

    location /health-gateway {
        default_type application/json;
        return 200 '{"status":"API Gateway berjalan"}';
    }
        
    location /users/ {
        proxy_pass http://user-service:4000/users/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }

    location /tasks/ {
        proxy_pass http://task-service:4001/tasks/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }

    location /health-users {
        proxy_pass http://user-service:4000/health;
    }

    location /health-tasks {
        proxy_pass http://task-service:4001/health;
    }

    location /groups/ {
        proxy_pass http://group-task-service:4002/groups/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }

    location /health-groups {
        proxy_pass http://group-task-service:4002/health;
    }

    location /progress/ {
        proxy_pass http://progress-service:4003/progress/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }

    location /health-progress {
        proxy_pass http://progress-service:4003/health;
    }

    location /notifications/ {
        proxy_pass http://notification-service:4004/notifications/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }

    location /health-notifications {
        proxy_pass http://notification-service:4004/health;
    }
        
    location /logs/ {
        proxy_pass http://logging-service:4005/logs/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }

    location /health-logs {
        proxy_pass http://logging-service:4005/health;
    }

    location /class/ {
        proxy_pass http://class-service:4006/class/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }

    location /health-class {
        proxy_pass http://class-service:4006/health;
    }

    location /socket.io/ {
        proxy_pass http://notification-service:4004;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;

        # # 🔥 Tambahkan CORS di sini
        # add_header Access-Control-Allow-Origin http://localhost:3000;
        # add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
        # add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept, Authorization";
        # add_header Access-Control-Allow-Credentials "true";
    }
    # Frontend React
    # location / {
    #     root /usr/share/nginx/html;
    #     index index.html;
    #     try_files $uri /index.html;
    # }

        # location / {
        #     return 404;
        # }


    }
